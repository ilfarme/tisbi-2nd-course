unit Unit5;

{$mode objfpc}{$H+}

interface

uses
  Classes, SysUtils, FileUtil, Forms, Controls, Graphics, Dialogs, StdCtrls,
  Menus;

type

  { TResults }

  TResults = class(TForm)
    TrueAnswers: TLabel;
    Fails: TLabel;
    TrueAnswersAmount: TLabel;
    Button1: TButton;
    CloseResultBtn: TButton;
    AmountOfQuestions: TLabel;
    QuestionsAmount: TLabel;
    Results: TLabel;
    TrueAnswersAmount1: TLabel;
    YourResult: TLabel;
    procedure FormCreate(Sender: TObject);
    procedure TrueAnswersClick(Sender: TObject);
    procedure YourResultClick(Sender: TObject);
  private
    { private declarations }
  public
    { public declarations }
  end;

var
  Results: TResults;

implementation

{$R *.lfm}

{ TResults }

procedure TResults.FormCreate(Sender: TObject);
var q,t,f:byte;
begin

end;

procedure TResults.TrueAnswersClick(Sender: TObject);
begin

end;

procedure TResults.YourResultClick(Sender: TObject);
begin

end;

end.

